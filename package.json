{
  "name": "my-test-project",
  "version": "0.0.1",
  "devDependencies": {
    "grunt": "~0.4.5",
    "grunt-contrib-jshint": "~0.10.0",
    "grunt-contrib-nodeunit": "~0.4.1",
    "grunt-contrib-uglify": "~0.5.0"
  },
  "scripts": {
    "build": "pwd && node --version && npm --version && npm config set prefix \"./npm/node_modules\" && export PATH=\"$PATH:./npm/node_modules/bin\" && npm install -g grunt-cli && npm install && grunt --version  && grunt"
  }
}
